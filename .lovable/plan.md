Add a "CSAE Policy" link in the homepage footer (`src/pages/Index.tsx`, lines 182–193) alongside the existing Privacy Policy and Terms of Service links, pointing to `/csae-policy`. This makes the policy publicly discoverable and crawlable by Google from the site's root page.

No other changes needed — the page itself already exists at `/csae-policy`.